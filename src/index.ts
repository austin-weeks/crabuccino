export { type Option, None, Some, some, none } from "./option";
export { type Result, Ok, Err } from "./result";
export { ResultAsync } from "./result-async";

/** Utility functions. */
const crab = {} as const;

export default crab;
